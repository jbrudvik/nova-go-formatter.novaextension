{
  "actions": {
    "build": {
      "enabled": true,
      "script": "npm install && npm run format && npm run lint && npm test"
    },
    "clean": {
      "enabled": true,
      "script": "npm run clean"
    }
  },
  "openLogOnRun": "start"
}
